// SCAFFOLD (routing Phase 1) — boilerplate + funnel wiring = Phase 2; real auth = separate auth spec.
export default function SetupPage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center gap-1 p-8 text-center">
      <h1 className="text-2xl font-semibold text-neutral-800">Profile setup — scaffold</h1>
      <p className="text-sm text-neutral-600">Placeholder (/setup). Boilerplate: Phase 2.</p>
    </div>
  );
}
