// SCAFFOLD (routing Phase 1) — voter chrome + token seam = Phase 4; 5-screen ballot = voter-flow spec.
export default function VoteBallotPage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center gap-1 p-8 text-center">
      <h1 className="text-2xl font-semibold text-neutral-800">Vote ballot — scaffold</h1>
      <p className="text-sm text-neutral-600">Placeholder (/vote/[token]). Voter chrome: Phase 4.</p>
    </div>
  );
}
